services:
  tests:
    build:
      context: .
    container_name: my-tests

    working_dir: /app

    volumes:
      - ./reports:/app/reports:rw

    user: root

    network_mode: host

    dns:
      - 8.8.8.8
      - 1.1.1.1