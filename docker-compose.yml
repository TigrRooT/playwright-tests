services:
  my-tests:
    build: .

    volumes:
      - .:/app
      - ./reports:/app/reports

    working_dir: /app

    environment:
      - BASE_URL=http://host.docker.internal:8080

    extra_hosts:
      - "host.docker.internal:host-gateway"

    command: ["bash", "./run_tests.sh"]