services:
  tests:
    build:
      context: .
      network: host

    container_name: my-tests

    #ВАЖНО: синхронизация пользователя
    user: "1000:1000"

    volumes:
      - ./reports:/app/reports

    network_mode: host

    dns:
      - 8.8.8.8
      - 1.1.1.1