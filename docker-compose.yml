services:
  tests:
    build:
      context: .
    container_name: my-tests
    volumes:
      - ./reports:/app/reports
    network_mode: host
    environment:
      BASE_URL: "http://localhost:8080"