services:
  tests:
    build:
      context: .
      network: host

    container_name: my-tests

    volumes:
      - ./reports:/app/reports

    network_mode: host

    environment:
      - PLAYWRIGHT_BROWSERS_PATH=/ms-playwright