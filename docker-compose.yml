services:
  app:
    image: your-app-image
    ports:
      - "8080:8080"

  tests:
    build:
      context: .
    depends_on:
      - app
    environment:
      BASE_URL: "http://app:8080"